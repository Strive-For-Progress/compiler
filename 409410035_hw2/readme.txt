How to compile my parser
    在終端機輸入 make compile

How execute my parser
    需先輸入 make compile 後，
    在終端機輸入 make run FILE={這裡放測試檔名}
    如: make run FILE=test1.c
        make run FILE=test2.c
        make run FILE=test3.c